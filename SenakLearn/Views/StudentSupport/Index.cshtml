
@model SenakLearn.Models.StudentSupport

@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    ViewBag.Title = "دانشجویان";
}
<!-- Page-Title -->
<div class="row">
    <div class="col-sm-12">
        <div class="page-title-box">
            <ol class="breadcrumb pull-right">
                <li><a href="#">@ViewBag.Title</a></li>
                <li class="active">Dashboard</li>
            </ol>
            @*<h4 class="page-title">@Html.ActionLink("افزودن", "Create") </h4>*@
        </div>
    </div>
</div>  

<div class="row">
    <div style="vertical-align: top;" class="col-xs-12 col-sm-12 col-md-4">
        <div id="TadGroupGoodsTreeDiv">



            <fieldset>
                @using MVC.Controls
                <div id="StudentSupport" style="width: 20%; height: 100%" class="span-3">
                </div>
                @(Html.TreeView1(new List<SenakLearn.Models.StudentSupport>(), "StudentSupport", "Id")
          .EmptyContent("هیچ موردی یافت نشد.")
          .IsAjax()
          .OpenAll(true)
          .ShowContextMenu(true)
          .showContextCreate(true, "createStudentSupportChild")
          .showContextEdit(true, "editStudentSupport")
          .showContextRemove(true, "deleteStudentSupport")
          .SetSearchable(true)
          .Sortable(false)
          //.ShowCheckbox(ViewBag.SelectedIds != null)
          .LanguageDirection("rtl")
          .AjaxUrl("/StudentSupport/GetTreeList?id=0", true))

                <script type="text/javascript">
                        function createStudentSupportChild(obj, id) {
                            $.ajax(
                                {
                                    type: 'Get',
                                    //data: { Code:  },
                                    url: '/StudentSupport/Create?id=' +
                                    (obj != null ? obj.id : id),
                                    success: function (result) {
                                        $('#StudentSupport').jstree("refresh");
                                        $('#OnlineClassDetailsDiv').html(result);
                                    },
                                    error: function (error) {
                                        alert(error.toString());
                                    }
                                });
                            return false;
                        }

                        function editStudentSupport(obj, id) {
                            $.ajax(
                                {
                                    type: 'Get',
                                    //data: { Code:  },
                                    url: '/StudentSupport/Edit?id=' + (obj != null ? obj.id : id),
                                    success: function (result) {
                                        $('#StudentSupport').jstree("refresh");
                                        $('#OnlineClassDetailsDiv').html(result);
                                    },
                                    error: function (error) {
                                        alert(error.toString());
                                    }
                                });
                            return false;
                        }

                        function deleteStudentSupport(obj) {
                            var deleted = function () {
                                $.post('/StudentSupport/Delete?id=' + obj.id,
                                     function (data) {
                                         $('#StudentSupport').jstree("refresh");
                                         //$("#OnlineClassFormId").reset();
                                         //  $("#NewBtn").trigger("click");
                                     });
                            }
                            confirmDelete(deleted);
                        }
                        function deleted() {

                            //$('#SubUnitTbl').hide();
                        }

                        $("#StudentSupport").delegate("a",
                            "click",
                            function (e) {
                                editStudentSupport(null, $(this).parent().attr('id'));
                                //$("#StudentSupport").jstree("open_node", $(this).parent());

                                //$("#StudentSupport").jstree("select_node", $(this).parent());
                                ////load process sequence
                                //loadOnlineClassByCode($(this).parent().attr('id'));

                                //lastCode = $(this).parent().attr('id');
                            });

                        $('#myForm').submit(function (e) {
                            
                            e.preventDefault();
                            var url = $(this).closest('form').attr('action'),
                     data = $(this).closest('form').serialize();
                            $.ajax({
                                url: url,
                                type: 'post',
                                data: data,
                                success: function () {
                                    $('#StudentSupport').jstree("refresh");
                                }
                            });
                        });
                </script>

            </fieldset>
        </div>
    </div>
    <div id="OnlineClassDetailsDiv" style="vertical-align: top;" class="col-xs-12 col-sm-12 col-md-8">
        @Html.Partial("~/Views/StudentSupport/Create.cshtml", new SenakLearn.Models.StudentSupport());
    </div>
</div>