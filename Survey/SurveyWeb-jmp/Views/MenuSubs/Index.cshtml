
@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    var urlList = "/MenuSubs/LoadList";
    ViewBag.Title = SurveyWeb.Models.Dictionary.ModelDictionary[typeof(SurveyWeb.Models.MenuSub)];
}
<!-- Page-Title -->
<div class="row">
    <div class="col-sm-12">
        <div class="page-title-box">
            <ol class="breadcrumb pull-right">
                <li><a href="#">@ViewBag.Title</a></li>
                <li class="active">Dashboard</li>
            </ol>
            <h4 class="page-title">@Html.ActionLink("افزودن", "Create") </h4>
        </div>
    </div>
</div>
<div id="TadGoodsField">
    @using MVC.Controls

    @Html.Grid(
    new GridControl().SetName("MenuSubGrid").
    SetPageSize(10).
    SetShrinkToFit(false).
    SetIsAutoSize(true).
    SetIsFilterToolbar(true).
    IsRTL().
    SetListUrl(urlList).
        SetCaption((string)ViewBag.Title).
    SetOnLoadCompleteEvent("loadMenuSubGridCompleteEvent").
        UseColumns(SurveyWeb.Controllers.MenuSubsController.MenuSubColumns)
)
    <script type="text/javascript">
	function loadMenuSubGridCompleteEvent() {
		var ids = $("#MenuSubGrid").jqGrid('getDataIDs');
		for (var i = 0; i < ids.length; i++) {
			var cl = ids[i];
			ed = '<a class="ajaxLink"  href="/MenuSubs/Create?Id=' + cl + '" title="ویرایش"><div class="fa fa-pencil"></div></a>';
			dl = '<a class="ajaxLink delete"href="/MenuSubs/Delete?Id=' + cl + '" title="حذف"   ><div class="fa fa-times"></div></a>';
		    de = '<a class="ajaxLink "href="/MenuSubs/Details?Id=' + cl + '" title="جزئیات"   ><div class="fa fa-building"></div></a>';
		    $('#MenuSubGrid').jqGrid('setRowData', ids[i], { act: ed + dl + de });
		}
    }

    </script>
</div>




