@model Tuple<int, SurveyWeb.Models.CartableType>

@{
    var urlList = "/CartableLogs/LoadList?id="+Model.Item1+ "&CartableType="+Model.Item2;
    ViewBag.Title = "لاگ کارتابل";
    Layout = null;
}
<!-- Page-Title -->
@*<div class="row">
    <div class="col-sm-12">
        <div class="page-title-box">
            <ol class="breadcrumb pull-right">
                <li><a href="#">@ViewBag.Title</a></li>
                <li class="active">Dashboard</li>
            </ol>
            <h4 class="page-title">@Html.ActionLink("افزودن", "Create") </h4>
        </div>
    </div>
</div>*@
<div id="TadGoodsField">
    @using MVC.Controls

    @Html.Grid(
    new GridControl().SetName("CartableLogGrid").
    SetPageSize(10).
    SetShrinkToFit(false).
    SetIsAutoSize(true).
    SetIsFilterToolbar(true).
    IsRTL().
    SetListUrl(urlList).
        SetCaption("لاگ کارتابل").
    SetOnLoadCompleteEvent("loadCartableLogGridCompleteEvent").
        UseColumns(SurveyWeb.Controllers.CartableLogsController.CartableLogColumns)
)
    <script type="text/javascript">
	function loadCartableLogGridCompleteEvent() {
		//var ids = $("#CartableLogGrid").jqGrid('getDataIDs');
		//for (var i = 0; i < ids.length; i++) {
		//	var cl = ids[i];
		//	ed = '<a class="ajaxLink"  href="/CartableLogs/Create?Id=' + cl + '" title="ویرایش"><div class="fa fa-pencil"></div></a>';
		//	dl = '<a class="ajaxLink delete"href="/CartableLogs/Delete?Id=' + cl + '" title="حذف"   ><div class="fa fa-times"></div></a>';
		//    de = '<a class="ajaxLink "href="/CartableLogs/Details?Id=' + cl + '" title="جزئیات"   ><div class="fa fa-building"></div></a>';
		//	$('#CartableLogGrid').jqGrid('setRowData', ids[i], { act: ed + dl+de  });
		//}
    }

    </script>
</div>




