@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    var urlList = "/PaymentTypes/LoadList";
    ViewBag.Title = "تنظيمات پرداخت";
}

<div class="row">
    <div class="col-sm-12">
        <div class="page-title-box">
            <ol class="breadcrumb pull-right">
                <li><a href="#">@ViewBag.Title</a></li>
                <li class="active">Dashboard</li>
            </ol>
            <h4 class="page-title">@Html.ActionLink("افزودن", "Create") </h4>
        </div>
    </div>
</div>
<div id="TadGoodsField">
    @using MVC.Controls

    @Html.Grid(
    new GridControl().SetName("PaymentTypeGrid").
    SetPageSize(10).
    SetShrinkToFit(false).
    SetIsAutoSize(true).
    SetIsFilterToolbar(true).
    IsRTL().
    SetListUrl(urlList).
        SetCaption("امورمالي").
    SetOnLoadCompleteEvent("loadPaymentTypeGridCompleteEvent").
        UseColumns(SurveyWeb.Controllers.PaymentTypesController.PaymentTypeColumns)
)
    <script >
	function loadPaymentTypeGridCompleteEvent() {
		var ids = $("#PaymentTypeGrid").jqGrid('getDataIDs');
		for (var i = 0; i < ids.length; i++) {
			var cl = ids[i];
			ed = '<a class="ajaxLink"  href="/PaymentTypes/Create?Id=' + cl + '" title="ویرایش"><div class="fa fa-pencil"></div></a>';
			dl = '<a class="ajaxLink delete"href="/PaymentTypes/Delete?Id=' + cl + '" title="آرشيو"   ><div class="fa fa-times"></div></a>';
		    de = '<a class="ajaxLink "href="/PaymentTypes/Details?Id=' + cl + '" title="جزئیات"   ><div class="fa fa-building"></div></a>';
		    $('#PaymentTypeGrid').jqGrid('setRowData', ids[i], { act: ed + dl + de });
		}
    }

    </script>
</div>




